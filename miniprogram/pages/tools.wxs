
var rtclass=function(e){
  var str='';
  var arr =e.products_class;
  var len = arr.length;
  for(var i=0;i<len;i++){
    str = str+' '+arr[i];
  }
  return str;
}
module.exports = {
  rtclass:rtclass
};